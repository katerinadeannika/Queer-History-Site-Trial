name: Import and render Chapter 14 bibliography
on:
  push:
    branches: [editorial-source]
    paths: ['.github/workflows/import-render-chapter14.yml']
permissions:
  contents: write
jobs:
  import-render:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install markdown
      - run: |
          curl -fsSL https://raw.githubusercontent.com/katerinadeannika/queer-history/main/Chapter%2014%20-%20Bibliography -o chapter-14-bibliography.md
          test "$(git hash-object chapter-14-bibliography.md)" = "903bcfb82c9c60cdab9078580c3cb15462b03b95"
      - run: |
          python - <<'PY'
          from pathlib import Path
          import markdown
          source = Path('chapter-14-bibliography.md').read_text(encoding='utf-8').strip()
          section = '## Chapter 14 — Bibliography\n\n' + source + '\n'
          for name in ['Before_the_Name_complete_master.md', 'Before_the_Name_1880-1969.md']:
              path = Path(name); text = path.read_text(encoding='utf-8')
              if '## Chapter 14 — Bibliography' not in text:
                  path.write_text(text.rstrip() + '\n\n---\n\n' + section, encoding='utf-8')
          body = markdown.markdown(section, extensions=['extra','sane_lists','tables'])
          out = f'''<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Chapter 14 — Bibliography | Early Queer History</title><link rel="stylesheet" href="site.css"></head><body><header class="site-header"><a href="index.html">Home</a><a href="chapters.html">Chapters</a><a href="research.html">Research</a><a href="bibliography.html">Bibliography</a></header><main class="page chapter">{body}<nav class="chapter-nav"><a href="chapter-13-further-reading.html">← Previous chapter</a></nav></main><footer class="site-footer">Early Queer History · Chapter 14</footer></body></html>'''
          Path('chapter-14-bibliography.preview.html').write_text(out, encoding='utf-8')
          PY
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add chapter-14-bibliography.md chapter-14-bibliography.preview.html Before_the_Name_complete_master.md Before_the_Name_1880-1969.md
          git diff --cached --quiet || git commit -m "Import and render Chapter 14 bibliography"
          git push